name: Build 4K POS
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: 4K-POS-v5
        run: npm install --legacy-peer-deps

      - name: Bump patch version
        shell: bash
        run: |
          cd 4K-POS-v5
          CURRENT=$(node -p "require('./package.json').version")
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
          NEW="$MAJOR.$MINOR.$((PATCH + 1))"
          node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8'));p.version='$NEW';fs.writeFileSync('package.json',JSON.stringify(p,null,2)+'\n');"
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"
          git add package.json
          git commit -m "chore: bump version to $NEW [skip ci]"
          git push
          echo "Version bumped: $CURRENT → $NEW"

      - name: Inject extension code secret
        shell: bash
        run: |
          sed -i "s|__EXT_CODE_SECRET__|${{ secrets.EXT_CODE_SECRET }}|g" "4K-POS-v5/license.js"

      - name: Auto-commit source changes
        shell: bash
        run: |
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"
          git checkout -- 4K-POS-v5/license.js
          git add .
          if ! git diff --cached --quiet; then
            git commit -m "chore: auto-commit build assets [skip ci]"
            git push
          fi

      - name: Build exe
        working-directory: 4K-POS-v5
        run: npx electron-builder --win --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish fixed-name installer to "download" release
        working-directory: 4K-POS-v5
        shell: bash
        run: |
          EXE_PATH=$(find dist -maxdepth 1 -name "*.exe" | head -n 1)
          if [ -z "$EXE_PATH" ]; then
            echo "No se encontró ningún .exe en dist/"
            exit 1
          fi
          cp "$EXE_PATH" dist/4K-POS-Setup.exe

          gh release delete download -y || true

          gh release create download dist/4K-POS-Setup.exe \
            -t "Descarga directa" \
            --notes "Descarga directa - siempre apunta a la última versión publicada." \
            || gh release upload download dist/4K-POS-Setup.exe --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

